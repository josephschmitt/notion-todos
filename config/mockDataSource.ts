// Development config: switch between flat and hierarchical mock data
// Change this to test different data structures during development
// - false: Flat data (one status per group) - matches current behavior
// - true: Nested data (multiple statuses per group) - hierarchical structure
export const USE_NESTED_MOCK_DATA = false;
